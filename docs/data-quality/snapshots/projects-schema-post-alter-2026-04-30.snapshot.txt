CREATE TABLE `projects` (
  `id` int NOT NULL AUTO_INCREMENT,
  `name` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,
  `clientId` int NOT NULL,
  `status` enum('rascunho','consistencia_pendente','cnaes_confirmados','perfil_entidade_confirmado','assessment_fase1','assessment_fase2','onda1_solaris','onda2_iagen','diagnostico_corporativo','diagnostico_operacional','q_produto','q_servico','diagnostico_cnae','briefing','riscos','plano','dashboard','matriz_riscos','plano_acao','em_avaliacao','aprovado','em_andamento','concluido','arquivado') COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'rascunho',
  `planPeriodMonths` int DEFAULT NULL,
  `createdById` int NOT NULL,
  `createdByRole` enum('cliente','equipe_solaris') COLLATE utf8mb4_unicode_ci NOT NULL,
  `createdAt` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `completedAt` timestamp NULL DEFAULT NULL,
  `notificationFrequency` enum('diaria','semanal','apenas_atrasos','marcos_importantes','personalizada') COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'semanal',
  `notificationEmail` varchar(320) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `taxRegime` enum('simples_nacional','lucro_presumido','lucro_real') COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `businessType` varchar(255) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `companySize` enum('mei','micro','pequena','media','grande') COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `mode` enum('temporario','historico') COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'historico',
  `sessionToken` varchar(128) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `description` text COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `confirmedCnaes` json DEFAULT NULL,
  `currentStep` int NOT NULL DEFAULT '1',
  `briefingContent` text COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `riskMatricesData` json DEFAULT NULL,
  `actionPlansData` json DEFAULT NULL,
  `briefingStructured` json DEFAULT NULL,
  `scoringData` json DEFAULT NULL,
  `faturamentoAnual` int DEFAULT NULL,
  `decisaoData` json DEFAULT NULL,
  `questionnaireAnswers` json DEFAULT NULL,
  `companyProfile` json DEFAULT NULL,
  `operationProfile` json DEFAULT NULL,
  `taxComplexity` json DEFAULT NULL,
  `financialProfile` json DEFAULT NULL,
  `governanceProfile` json DEFAULT NULL,
  `diagnosticStatus` json DEFAULT NULL,
  `corporateAnswers` json DEFAULT NULL,
  `operationalAnswers` json DEFAULT NULL,
  `cnaeAnswers` json DEFAULT NULL,
  `currentStepName` varchar(64) COLLATE utf8mb4_unicode_ci DEFAULT 'perfil_empresa',
  `stepUpdatedAt` timestamp DEFAULT CURRENT_TIMESTAMP,
  `stepHistory` json DEFAULT NULL,
  `profileVersion` varchar(20) COLLATE utf8mb4_unicode_ci DEFAULT '1.0',
  `consistencyStatus` enum('pending','analyzing','ok','warning','blocked') COLLATE utf8mb4_unicode_ci DEFAULT 'pending',
  `consistencyAcceptedRiskBy` int DEFAULT NULL,
  `consistencyAcceptedRiskAt` timestamp NULL DEFAULT NULL,
  `consistencyAcceptedRiskReason` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `briefingContentV1` text COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `briefingContentV3` text COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `riskMatricesDataV1` json DEFAULT NULL,
  `riskMatricesDataV3` json DEFAULT NULL,
  `actionPlansDataV1` json DEFAULT NULL,
  `actionPlansDataV3` json DEFAULT NULL,
  `product_answers` json DEFAULT NULL,
  `service_answers` json DEFAULT NULL,
  `solaris_skipped_ids` text COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'ADR-0016: JSON string[] — IDs de perguntas SOLARIS puladas',
  `iagen_skipped_ids` text COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT 'ADR-0016: JSON string[] — IDs de perguntas IA Gen puladas',
  `solaris_skipped_all` tinyint(1) NOT NULL DEFAULT '0' COMMENT 'ADR-0016: true = usuário pulou questionário SOLARIS inteiro',
  `iagen_skipped_all` tinyint(1) NOT NULL DEFAULT '0' COMMENT 'ADR-0016: true = usuário pulou questionário IA Gen inteiro',
  `archetype` json DEFAULT NULL,
  `archetypeVersion` varchar(20) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `archetypePerfilHash` varchar(64) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `archetypeRulesHash` varchar(64) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `archetypeConfirmedAt` timestamp NULL DEFAULT NULL,
  `archetypeConfirmedBy` int DEFAULT NULL,
  PRIMARY KEY (`id`) /*T![clustered_index] CLUSTERED */
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci AUTO_INCREMENT=2310001
