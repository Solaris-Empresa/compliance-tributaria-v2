# Auditoria de Fim de Sessão — v7.63 · 2026-05-05 · Sprint M3.8.1 HOTFIX ENCERRADA

**Gatilho disparado:** Hotfix P0 (REGRA-ORQ-11) · 2 PRs mergeados (#973 + closure)
**Atores:** Claude Code (impl + audit) · Manus (diagnóstico + smoke E2E + ALTER TABLE prod) · P.O. (autorização autônoma)
**Predecessor:** v7.62 (`2a6581c`) · Sprint M3.8 encerrada
**HEAD origin/main pré-closure:** `5d94c9c` (PR #973 squash)

---

## Contexto

Manus identificou em 2026-05-05 (post-deploy M3.8) **3 bugs reais** durante smoke E2E:

| Bug | Severidade | Origem |
|---|---|---|
| A | P0 (perda dados) | `gapEngine.ts:459` wipe destrutivo — pré-existente Z-11, exposto por M3.8 + auto-trigger |
| B | P1 (UI errada) | `risk-engine-v4.ts:285` default `"iagen"` — introduzido por PR #968 (M3.8-1B) |
| C | P2 (type mismatch) | `risk-engine-v4.ts:37` `Fonte` sem `"regulatorio"` — introduzido por PR #968 (M3.8-1B) |

Bug A causava perda silenciosa de gaps `solaris`/`iagen` a cada visita ao RiskDashboardV4 — race contra fire-and-forget. Bug B mascarava todas as fontes como `"iagen"` na UI.

---

## Passo 0 — Sincronia local (Claude Code)

```
git fetch origin --prune
git rev-parse origin/main
→ 5d94c9c5571321ff739a8726b134bb0aada2444f (pré-followup ENUM)
```

Branch ativa: `chore/m3.8.1-followup` em sync com `origin/main`.

---

## Passo 1 — 4 HEADs alinhados (Manus, validado pós-deploy)

✅ Manus reportou 2026-05-05:
- github/main: `5d94c9c` (CONFIRMADO)
- iasolaris.manus.space sandbox: republish concluído
- Production deploy: ALTER TABLE manual aplicado (formaliza em PR #974)

---

## Passo 2 — Inventário de PRs Sprint M3.8.1 (Claude Code)

| PR | SHA | Mergeado | Tipo | Conteúdo |
|---|---|---|---|---|
| **#973** | `5d94c9c` | 2026-05-04 21:42Z | fix | Hotfix consolidado A+C+B + 16 tests + script retrigger |
| **#974** (este) | TBD | TBD | chore | Migration 0091 ENUM + audit v7.63 + ESTADO-ATUAL + Lição #64 |

**Total:** 2 PRs. Issue #972 (closes via #973).

---

## Passo 3 — Greps de artefatos (Claude Code)

### Bug A scoped DELETE
- `server/routers/gapEngine.ts:464` agora contém `AND analysis_version = 3 AND source = 'v1'`
- Outros 3 analyzers (`solaris-gap-analyzer`, `iagen-gap-analyzer`, `engine-gap-analyzer`) já usavam scoped DELETE — consistência alcançada

### Bug B + C runtime fix
- `server/lib/risk-engine-v4.ts:37` `type Fonte` inclui `"regulatorio"`
- `server/lib/risk-engine-v4.ts:100-110` `SOURCE_RANK.regulatorio = 6`
- `server/lib/risk-engine-v4.ts:289` default `"regulatorio"` (era `"iagen"`)
- `server/lib/risk-engine-v4.ts:288` `getBestSourcePriority` agora exportado para tests

### Schema alignment (PR #974 — este)
- `drizzle/0091_risks_v4_source_priority_regulatorio.sql` — ALTER TABLE adiciona `'regulatorio'` ao ENUM
- `drizzle/downs/0091_down.sql` — reversão idempotente (com pré-condição UPDATE)
- `server/lib/db-queries-risks-v4.ts:33` `SourcePriorityV4` += `"regulatorio"`

### Test contracts
- `server/lib/m3.8.1-hotfix.test.ts` (NOVO) — 16 tests
  - 3 Bug A (grep static + comentário + ausência DELETE wide)
  - 4 Bug C (SOURCE_RANK + preservação + rank máximo + assignability)
  - 7 Bug B (cenário #3480001 + array vazio + 4 cenários mistos + regression iagen)
  - 2 documentação inline

### Script de re-trigger
- `scripts/retrigger-pipeline.ts` — utilitário pós-merge (ESCOPO RESTRITO)
  - FAZ: DELETE gaps/risks/plans dos projetos + analyzeSolarisAnswers + analyzeIagenAnswers + resumo
  - NÃO FAZ: RAG, requirements, respostas, schema, outros projetos

---

## Passo 4 — HTTP prod (Manus, evidência empírica de banco)

### Smoke E2E completo — AMBOS OS PROJETOS 🟢

| Critério | #3480001 | #3270001 | Status |
|---|---|---|---|
| Riscos (era 9, agora 8) | 8 (6R + 2O) | 8 (6R + 2O) | ✅ |
| `enquadramento_geral` removido | 0 | 0 | ✅ |
| `source_priority` ≠ `"iagen"` único | 100% `"regulatorio"` | 100% `"regulatorio"` | ✅ |
| Gaps multi-fonte preservados (Bug A) | v1:138 + solaris:28 + iagen:7 | v1:138 + solaris:32 + iagen:9 | ✅ |
| `answer_value` preenchido (M3.8-2) | 38 gaps com valor | 44 gaps com valor | ✅ |

**Validação Bug A:** evidência de gaps `solaris` (28 e 32) + `iagen` (7 e 9) coexistindo com gaps `v1` (138). Antes do fix, gaps solaris/iagen eram apagados a cada visita.

**Validação Bug B+C:** todos os riscos derivados de gaps regulatorio agora exibem `source_priority="regulatorio"` corretamente.

### Ação corretiva pelo Manus durante smoke

Smoke #3480001 falhou com `ERROR 1265: Data truncated for column 'source_priority'` no INSERT de risks_v4. Manus aplicou `ALTER TABLE` manual em produção:

```sql
ALTER TABLE risks_v4 MODIFY COLUMN source_priority ENUM(
  'cnae','ncm','nbs','solaris','iagen','regulatorio'
) NOT NULL;
```

Este PR #974 formaliza essa mudança via `drizzle/0091_*.sql` para preservar consistência em ambientes recriados (CI, dev, staging).

---

## Passo 5 — Unit tests locais (Claude Code)

```
pnpm vitest run server/lib/m3.8.1-hotfix.test.ts → 16/16 PASS
pnpm vitest run server/lib/risk-engine-v4.test.ts → 39/39 PASS (regression OK)
pnpm vitest run server/lib/risk-eligibility.test.ts → 28/28 PASS
pnpm vitest run server/lib/m3.8-3-eliminate-fallback.test.ts → 16/16 PASS
pnpm vitest run server/lib/m3.8-2-unified-answer.test.ts → 40/40 PASS
Total M3.8 + M3.8.1: 139/139 PASS
```

```
pnpm tsc --noEmit → 0 erros
```

---

## Passo 6 — Smoke UX (Manus, completado pré-closure)

✅ Concluído em Passo 4 acima — smoke E2E em produção com evidência de banco.

---

## Passo 7 — Veredito P.O.

🟢 **VERDE — Sprint M3.8.1 hotfix encerrada com sucesso**

### Consolidado tabular

| Métrica | Valor |
|---|---|
| **HEAD origin/main pré-followup** | `5d94c9c` |
| **HEAD origin/main pós-followup (este PR)** | TBD |
| **PRs auditados** | 2 (#973 hotfix + #974 followup) |
| **Greps de artefatos** | 4 categorias confirmadas (Bug A scoped, Bug B+C runtime, ENUM migration, tests) |
| **tsc --noEmit** | 0 erros |
| **vitest M3.8 + M3.8.1** | 139/139 PASS |
| **HTTP prod smoke** | 5/5 critérios PASS em ambos projetos |
| **Smoke UX manual** | 🟢 ambos projetos validados |

### Bugs/bloqueadores residuais

🟡 **Backlog M3.10** (não bloqueia):
- Race condition fire-and-forget em `routers-fluxo-v3.ts:4027` (post-mortem)
- M3.9-1: Curadoria SOLARIS `risk_category_code` (24 perguntas)
- M3.9-3: Curadoria `requirement_question_mapping` (138+)
- M3.9-A/B/C: Pipeline Q.SOLARIS/Q.NBS para fontes diferentes de iagen na matriz (issue UX badge)

### Próximos passos

| # | Ator | Ação |
|---|---|---|
| 1 | **Claude Code** | Mergear PR #974 (este) — ENUM + audit + Lição #64 |
| 2 | **Manus** | Smoke pós-#974 (CI verde, ambiente novo aplica migration corretamente) |
| 3 | **Equipe SOLARIS jurídica** | Iniciar curadoria M3.9-1 e M3.9-3 (paralelo) |
| 4 | **Claude Code** | Aguardar curadoria → ativar M3.9-4/5/6 (stubs UnifiedAnswer) |

### Lições aplicadas + capturadas

**Aplicadas pré-existentes:**
- REGRA-ORQ-11 (fast-track P0) — justificou triade leve em vez de full
- REGRA-ORQ-22 (3 níveis de crítica) — Manus crítica em 3 níveis no PR
- REGRA-ORQ-27 (Lição #59 assemble ≠ consumption) — fundamentou test contracts runtime
- REGRA-ORQ-28 (triade) — aplicada na versão leve

**Lição NOVA capturada (#64):** PR docs-only audit-greps são insuficientes para detectar contratos quebrados — Bug B/C foram introduzidos por PR #968 (M3.8-1B) e o audit v7.62 validou apenas remoção de hardcode `"solaris"`, não cobertura runtime do consumo downstream. Reforça REGRA-ORQ-27 e adiciona ao processo de auditoria a obrigatoriedade de runtime tests para PRs que alteram retornos de funções consumidas por outros módulos.

---

**Arquivado em:** `docs/governance/audits/v7.63-2026-05-05-sprint-m3.8.1-hotfix-encerrada.md`
**Vinculado a:** PR #973 (impl) + PR #974 (followup ENUM + audit + Lição #64)
