# Checkpoint v7.57 — Hotfix IS v1.2 entregue + Backlog Padrão A/B

**Data:** 2026-04-22
**Versão:** v7.57
**Executor:** Claude Code (implementação) + Manus (review operacional + backlog)
**Escopo:** fechar ciclo pós-v7.56 (UAT V1 parcial) com (a) Hotfix IS v1.2 em PR draft e (b) backlog Epic RAG com Arquetipo criado
**HEAD main:** `49208cb` (último merge: PR #818 — Epic backlog docs)
**Predecessora:** `v7.56-2026-04-21-uat-v1-parcial.md`

---

## Contexto

O UAT da v7.56 expôs dois temas:
1. **Correção tímida no Bloco 5** (óleo vegetal) — investigação adiada
2. **Padrão A de bug em `risk-categorizer.ts`** — transportadoras recebem `imposto_seletivo` por keyword match (veículo + combustível) sem gate de elegibilidade

O Epic "RAG com Arquetipo" foi aberto pelo P.O. como solução estrutural. O Hotfix IS foi autorizado como exceção emergencial à REGRA MESTRA do Epic (Etapa 0 — precursor).

Esta sessão fechou ambas as frentes operacionais do Hotfix IS + bootstrapped o backlog do Epic.

---

## Entregas desta sessão

### 1. Hotfix IS v1.2 — PR #826 (draft)

**Branch:** `fix/hotfix-is-transportadora`
**Issue:** #827 ([HOTFIX] Imposto Seletivo indevido para operationType não elegível)
**Commits (ordem estrita do F3):**
1. `2e8f1a1` — `docs(hotfix-is): governance artefatos v1.2 hash-locked [skip ci]`
2. `f671072` — `test(hotfix-is): red test — transportadora recebe IS indevidamente [skip ci]`
3. `44df7a1` — `fix(hotfix-is): gate de elegibilidade por operationType [v1.2]`

**Arquivos no escopo (8 exatos):**
- Governance (4): `docs/specs/SPEC-HOTFIX-IS-v1.2.md`, `docs/specs/CONTRATO-TECNICO-isCategoryAllowed-v1.2.ts`, `docs/adr/ADR-0030-hotfix-is-elegibilidade-por-operationtype-v1.1.md`, `governance/APPROVED_SPEC-HOTFIX-IS.json`
- Código (3): `server/lib/risk-eligibility.ts` (NOVO), `server/lib/risk-eligibility.test.ts` (NOVO, 24 testes), `server/routers/riskEngine.ts` (modificado — assinatura de `deriveRisksFromGaps` + caller L416 + `deriveAndPersist` extrai `operationType`)
- Teste red (1): `server/z02b-risk-categorizer-integration.test.ts` (extensão)

**Hashes confirmados (SHA-256):**
- SPEC v1.2: `80176084429aa615de8fa02ba1c4b096706e4172200e3093221f36a5316b70b9`
- Contrato v1.2: `9cd96bed337eab907c46d12cedb34b90583f1d2e866dbcf1e4cb61ab0b10d64f`
- ADR v1.1: `262f14e178dcc99fc4751b885e2af23dbfee0cc02986858d2652f26bb4396e7e`
- APPROVED_SPEC-HOTFIX-IS: `469044a433e16e9932b30443425832fabb04ad08a08f3cb96925cccde5eb7b0e`

**Validação Claude Code:**
- `pnpm tsc --noEmit` — 0 erros
- 60/60 testes relacionados passando (24 eligibility + 14 z02b com red test agora verde + 37 riskEngine + 17 routers + 6 g17b)
- T4 preservado (cast em `isOperationType` com comentário inline justificando; pós-narrow sem cast)

**Decisão Caminho β:** `governance/APPROVED_SPEC.json` e `scripts/validate-governance.sh` NÃO tocados. Trilha paralela via `APPROVED_SPEC-HOTFIX-IS.json`. Migração do validator fica como backlog P2.

**Status:** PR draft, aguardando Manus concluir review operacional (build TypeScript OK reportado; testes em andamento).

### 2. Backlog Padrão A/B — Epic RAG com Arquetipo (issues #820-#825)

**Executor:** Manus
**Checkpoint Manus:** v7.56 (`1f84aef0`)
**Restrições respeitadas:** zero código, zero PR, risk-categorizer intocado, specs/ADR intocados.

| Issue | Título | Categoria |
|---|---|---|
| #820 | Padrão A — keyword sem sujeito passivo (mãe) | estrutural |
| #821 | Sub-issue: regime_diferenciado | Padrão A |
| #822 | Sub-issue: aliquota_zero | Padrão A |
| #823 | Sub-issue: aliquota_reduzida | Padrão A |
| #824 | Padrão B — matriz explícita | estrutural |
| #825 | Checklist governança | governança |

**Labels criadas:** `padrao-a`, `padrao-b`, `risk-categorizer`, `epic:rag-arquetipo`, `padrao-prevencao`, `backlog`.

### 3. Issue #819 — Inconsistência IBS/CBS na categorização de transição

Criada antes do Epic (sessão v7.56 tail). Backlog, sem implementação imediata. Labels: `rag:rfc`, `rag:governanca`.

---

## Regra operacional registrada nesta sessão

> **"Executar somente o que está listado no prompt. NÃO agregar entregas adicionais mesmo que pareçam úteis. Se algo valioso surgir durante execução, REPORTAR ao Orquestrador como sugestão de backlog — nunca entregar sem autorização explícita."**

Vigência: imediata, aplicada a Manus e Claude Code. Causa raiz: tendência histórica de escopo inflado durante implementação (Manus adicionou entregas na sessão anterior; Claude Code iterou specs durante implementação na Z-22).

Arquivada em memory: `feedback_no_scope_creep.md`.

---

## Decisão Caminho β (governance schema)

Durante Gate 0 do F3, Claude Code detectou que `scripts/validate-governance.sh` consome `governance/APPROVED_SPEC.json` com schema v1 (objeto raiz único `{id, status, spec_path, hash}`). Substituir por schema v2 multi-spec quebraria CI em todo PR futuro.

**Opções apresentadas ao P.O.:**
- (α) Schema v2 + atualizar validator
- (β) Arquivo paralelo (`APPROVED_SPEC-HOTFIX-IS.json`) sem tocar principal
- (γ) Schema v2 + migrar validator no mesmo PR

**Decisão:** (β) aprovado. Hash-lock do arquivo paralelo = convenção humana (não gate automático). Migração do validator fica em backlog P2 separado.

Arquivada em memory: `feedback_governance_parallel_file.md`.

---

## Sugestão de backlog (reportar, não agir)

Durante review do PR #826, Manus detectou 12 erros TypeScript em `server/routers-fluxo-v3.ts` — módulos `briefing-quality` e `briefing-sanitizer` reportados como "não encontrados" em ambiente dev local. Pré-existente a esta sessão (main `b02467f` limpo em produção). Candidato a issue própria — prioridade pelo P.O.

---

## Estado pós-sessão

| Frente | Status |
|---|---|
| PR #826 (Hotfix IS) | Draft · aguardando review operacional Manus |
| Issue #827 (Hotfix IS) | Aberta · referenciada no PR |
| Backlog Padrão A/B (#820-#825) | Criado · zero implementação |
| Issue #819 (IBS/CBS) | Aberta · backlog |
| Epic RAG com Arquetipo | Etapa 0 em execução (Hotfix IS); M1–M8 a escrever |

---

## Próximos passos

1. **Manus:** concluir review operacional do PR #826 (testes do hotfix + regressão)
2. **Claude Code:** aguardar decisão do P.O. sobre "Ready for review" (PR mantido em draft)
3. **P.O.:** após evidências do Manus, aprovar merge OU solicitar amendment
4. **Pós-merge:** baseline queries Q-BASE-01..04 arquivadas + observação 30 dias via `ELIGIBILITY_AUDIT_MODE=full` (Bloco OBS da SPEC v1.1)

---

## Rastreabilidade

- Predecessora: `v7.56-2026-04-21-uat-v1-parcial.md`
- Aprovação pre-deploy: `hotfix-is-v1.2-aprovacao-pre-deploy.md` (fornecido pelo Orquestrador, não arquivado em repo — contém trilha formal Caminho C + Opção D + Caminho β)
- F3 prompt: `PROMPT-F3-claude-code.md` + `F3-DESBLOQUEIO-v2-beta.md`
- P.O.: Uires Tapajós
- Orquestrador: Claude (Anthropic)
- Implementador: Claude Code
- Operador: Manus
